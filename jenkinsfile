pipeline {
    agent any
    stages{
        stage("build code"){
            steps {
            sh"""
               ./mvnw package -DskipTests
                ls -lrt target/*.jar
            """
            }
        }
    }
}