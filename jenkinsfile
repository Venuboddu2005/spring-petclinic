pipeline {
    agent any

    stages {
        stage("Build") {
            steps {
                echo "Building..."
                stage("Run-Unit-Tests") { 
                    steps {
                       sh 'mvn test'
                    }
                }
            }
        }
    }
}
